<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aioa.workflow.mapper.ApprovalMapper">

    <resultMap id="BaseResultMap" type="com.aioa.workflow.entity.Approval">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="type" property="type"/>
        <result column="content" property="content"/>
        <result column="status" property="status"/>
        <result column="priority" property="priority"/>
        <result column="applicant_id" property="applicantId"/>
        <result column="applicant_name" property="applicantName"/>
        <result column="approver_id" property="approverId"/>
        <result column="approver_name" property="approverName"/>
        <result column="dept_id" property="deptId"/>
        <result column="dept_name" property="deptName"/>
        <result column="cc_users" property="ccUsers"/>
        <result column="expect_finish_time" property="expectFinishTime"/>
        <result column="finish_time" property="finishTime"/>
        <result column="current_step" property="currentStep"/>
        <result column="total_steps" property="totalSteps"/>
        <result column="attachments" property="attachments"/>
        <result column="form_data" property="formData"/>
        <result column="remark" property="remark"/>
        <result column="approval_comment" property="approvalComment"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="create_by" property="createBy"/>
        <result column="update_by" property="updateBy"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <select id="selectByApplicantId" resultMap="BaseResultMap">
        SELECT * FROM approval
        WHERE deleted = 0 AND applicant_id = #{applicantId}
        ORDER BY create_time DESC
    </select>

    <select id="selectPendingByApproverId" resultMap="BaseResultMap">
        SELECT * FROM approval
        WHERE deleted = 0 AND approver_id = #{approverId} AND status = 0
        ORDER BY create_time DESC
    </select>

    <select id="selectByStatus" resultMap="BaseResultMap">
        SELECT * FROM approval
        WHERE deleted = 0 AND status = #{status}
        ORDER BY create_time DESC
    </select>

    <select id="searchByKeyword" resultMap="BaseResultMap">
        SELECT * FROM approval
        WHERE deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (title LIKE CONCAT('%', #{keyword}, '%') OR content LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="applicantId != null and applicantId != ''">
            AND applicant_id = #{applicantId}
        </if>
        <if test="approverId != null and approverId != ''">
            AND approver_id = #{approverId}
        </if>
        ORDER BY create_time DESC
    </select>

    <select id="countByApplicantId" resultType="long">
        SELECT COUNT(*) FROM approval WHERE deleted = 0 AND applicant_id = #{applicantId}
    </select>

    <select id="countPendingByApproverId" resultType="long">
        SELECT COUNT(*) FROM approval
        WHERE deleted = 0 AND approver_id = #{approverId} AND status = 0
    </select>

    <select id="selectOverdue" resultMap="BaseResultMap">
        SELECT * FROM approval
        WHERE deleted = 0
        AND status = 0
        AND expect_finish_time IS NOT NULL
        AND expect_finish_time &lt; NOW()
        ORDER BY expect_finish_time ASC
    </select>

</mapper>
